services:
  # NOTE: サービス名は共有ネットワーク(app-network)上のDNSエイリアスになるため、
  # anti-yt側の`backend`サービスと衝突しない一意な名前にすること
  kioku-backend:
    build:
      context: ./backend
    image: kioku-backend
    container_name: kioku-backend
    expose:
      - "3000"
    env_file:
      - .env
    secrets:
      - jwt-private
      - jwt-public
    restart: unless-stopped
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
    container_name: kioku-frontend
    volumes:
      - frontend-dist:/output
    restart: "no"

volumes:
  frontend-dist:
    external: true
    name: kioku-frontend-dist

secrets:
  jwt-private:
    file: ./secrets/jwt_private.pem
  jwt-public:
    file: ./secrets/jwt_public.pem

networks:
  app-network:
    external: true
